package first.client.playercharacter.CharacterClasses.Mage;

public enum ActionMethods {
//this enum will contain the different action methods available to the Knight class
//it is mostly shorthand for within the package
//it is simply a way to organize the different action methods available to the packaged character class
}
