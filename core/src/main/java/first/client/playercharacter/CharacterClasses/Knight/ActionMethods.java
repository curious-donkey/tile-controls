package first.client.playercharacter.CharacterClasses.Knight;

public enum ActionMethods {
//this enum will contain the different action methods available to the Knight class
//it is mostly shorthand for within the package
//it will not be used outside of the package
//unlike the Animations enum, this enum is not tied to graphics rendering
       //as such, it does not need to treated as an api by the character controller
       //and the animations enum is enormous, so it better to automate calling them
       //the attacks method could be tied to a generic method in an abstract class
       //but it is not necessary at this time
//it is simply a way to organize the different attack methods available to the packaged character class
    
}
